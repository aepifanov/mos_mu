---

- include: verify.yml skip_show_verify_result=false

- hosts: all:!fuel
  gather_facts: false

  vars_files:
    - "vars/7.0.yml"

  tasks:
    - include: tasks/pkgs_get_customizations.yml
      when: verify_result.stdout != ""

